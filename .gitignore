venv/
__pycache__/
*.pyc

# Credenciales. NUNCA se sube: el repo es PÚBLICO.
.env

# Salidas de cada corrida: se regeneran solas y no tienen por qué vivir en el repo.
salida/
salida_reels/

# Mediciones del sondeo de medios: se rehacen corriendo tools/sondear_medios.py.
data/sondeo*.json
data/ranking.json
data/cobertura_regional.json
data/candidatos_directorio.json

# ⚠️ `estado/` NO se ignora a propósito: ahí vive el ledger de notas ya usadas, y es lo
# ÚNICO que tiene que sobrevivir entre las pasadas. Cada corrida de GitHub Actions arranca
# en una máquina vacía, así que si esto se ignorara, el sistema repetiría los mismos reels
# tres veces por día.
