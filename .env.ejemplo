# Copiar a .env y completar. El .env esta en .gitignore: no se sube.
# Sin esta clave el scraper corre igual con --sin-ia (solo diccionario, gratis),
# pero la salida trae falsos positivos.
ANTHROPIC_API_KEY=sk-ant-...
